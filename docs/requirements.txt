# Documentation-only dependencies (not part of dim_red's runtime deps).
# Install into the dmred env with: pip install -r docs/requirements.txt
sphinx>=8
furo
myst-parser
sphinx-design
sphinx-copybutton

# Real (non-mocked) imports needed by the autodoc'd modules -- cheap, wheel-only
# packages. Everything heavier (jax, dscribe, pymatgen, ...) is listed in
# conf.py's autodoc_mock_imports instead, so the docs build never needs the
# full dmred scientific-stack environment (see .github/workflows/docs.yml).
pyyaml
numpy
matplotlib
scikit-learn
